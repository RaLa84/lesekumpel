name: Generate Sitemap

on:
  push:
    branches: [main]
    paths:
      - 'texte/**.html'
      - 'comicgeschichten/**.html'
      - 'demo-texte/**.html'
      - 'index.html'
      - 'landingpage.html'
      - 'demo.html'
      - 'preise.html'
      - 'ueber-uns.html'
      - 'blog-warum-lesekumpel.html'
      - 'neue-geschichte.html'
      - 'starter.html'
      - 'impressum.html'
      - 'datenschutz.html'
      - 'scripts/generate-sitemap.mjs'
      - 'scripts/generate-stories-index.mjs'
  workflow_dispatch:

jobs:
  sitemap:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          # Persist credentials so the auto-commit at the end can push back.
          persist-credentials: true

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate sitemap.xml + stories-index.json
        run: |
          node scripts/generate-sitemap.mjs
          node scripts/generate-stories-index.mjs

      - name: Commit sitemap + index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sitemap.xml stories-index.json
          if git diff --cached --quiet; then
            echo "Keine Änderungen an sitemap.xml / stories-index.json"
          else
            git commit -m "Auto: sitemap.xml + stories-index.json regeneriert"
            git pull --rebase
            git push
          fi
